{
  "name": "balance-erp-analiz",
  "version": "1.0.0",
  "description": "Balance ERP Proje Analiz Aracı",
  "scripts": {
    "db": "json-server --watch db.json --port 3001",
    "serve": "npx serve -p 5500 .",
    "start": "concurrently \"npm run db\" \"npm run serve\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
